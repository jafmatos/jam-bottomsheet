{
	"name": "testing-react-native-bottomsheet",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
    "test:build": "tsc --project tsconfig.json && tscpaths -p tsconfig.json -s ./src -o ./dist"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": "",
	"devDependencies": {
		"tsconfig-paths": "^4.2.0",
		"tscpaths": "^0.0.9",
		"typescript": "^6.0.3"
	},
	"dependencies": {
		"react-native-bottomsheet": "file:../react-native-bottomsheet-0.1.0.tgz"
	}
}
